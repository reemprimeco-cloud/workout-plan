When the user taps the "Sign in with Google" button, a loading spinner appears directly on the button itself. This loading state consists of a continuous circular spinner. The loading does not complete; it remains stuck in this spinning state indefinitely without showing any error message or progressing to the next screen.